# How to Run

- `python3 -m bot`

with dokcer:

- `docker compose up --build -d`

with Makefile:

- `make up`
- `make down`
- `make restart`
- `make logs`

## Freeze requirement

- `pip freeze > requirement.txt`
